#include "../include/trie.h"
#include <stdlib.h>
#include <stdbool.h>

void insert(TrieNode* root, const char* header_key) {

}

bool search(TrieNode* root, const char* header_key) {
  return true;
}

TrieNode* trie_header_init() {
  TrieNode* root = malloc(sizeof(TrieNode));  
  return root;
}




