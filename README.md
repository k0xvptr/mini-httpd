# Mini-httpd
A Minimal, high performance HTTP server that supports HTTP/1.1 and written in C

# The Goal
The goal of this project is to explore what is behind the high level abstractions created by libraries like Django and Node.js. Writing in C makes manual memory management, native network libraries, and very minimal
